@model IEnumerable<DACS_WebClothes.Models.Inventory>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<div style="color:black">
    <h1 style="text-align:center">QUẢN LÝ KHO HÀNG</h1>
    <div class="search-field d-none d-xl-block" style="margin-top: 25px" ;>
        <form class="d-flex align-items-center" action="#">
            <div class="form-group mb-0">
                <input type="text" class="form-control search-input" placeholder="Tìm kiếm" name="SearchString">
                <button type="submit" class="btn btn-success search-button ml-2">Tìm Kiếm</button>
            </div>
        </form>
    </div>
    <div class="text-center" style="margin-top: -40px" ;>
        <button class="btn btn-create-new"><a href="~/Admin/AdminInventories/Create">Tạo Mới</a></button>
    </div>
    <section class="attendance">
        <div class="attendance-list">
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            Sản phẩm
                        </th>
                        <th>
                            Nơi nhập hàng
                        </th>
                        <th>
                            Ngày nhập hàng
                        </th>
                        <th>
                            Giá nhập hàng
                        </th>
                        <th>
                            Số hàng tồn
                        </th>
                        <th>
                            Ẩn/hiện
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Product.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PlaceOfImport)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ImportDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ImportPrice)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.InventoryNumber)
                            </td>
                            <td>
                                @(item.Hidden == 0 ? "Hiện" : "Ẩn")
                            </td>
                            <td>
                                <div class="text-center">
                                    <button class="btn btn-create-new" type="button" style="margin-top: 5px" ;>
                                        <a href="~/Admin/AdminInventories/Edit?id=@item.InventoryId">Sửa</a>
                                    </button>
                                    <button class="btn btn-create-new" type="button" style="margin-top: 5px" ;>
                                        <a href="~/Admin/AdminInventories/Details?id=@item.InventoryId">Xem chi tiết</a>
                                    </button>
                                    <button class="btn btn-create-new" type="button" style="margin-top: 5px" ;>
                                        <a href="~/Admin/AdminInventories/Delete?id=@item.InventoryId">Xóa</a>
                                    </button>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </section>
</div>